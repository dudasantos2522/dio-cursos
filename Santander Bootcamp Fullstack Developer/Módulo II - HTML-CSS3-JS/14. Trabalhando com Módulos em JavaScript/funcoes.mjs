function idade(nome, idade) {
    return `${nome} tem ${idade} anos`;
}

function cidade(nome, cidade) {
    return `${nome} é de ${cidade}`;
}

function hobby(nome, hobby) {
    return `${nome} gosta de ${hobby}`;
}

export {
    idade,
    cidade,
    hobby
}